<!-- Property details -->
<%= render property_listing %>

<%= link_to "edit", edit_property_path(property_listing) %>
| 
<%= link_to "delete", property_listing, method: :delete, confirm: "Are you sure? This will delete any history of service requests, both active and completed, for this property. It cannot be undone.", title: property_listing.name %>
<br>
<br>
Credit card on file for this property:<br>
<% if property_listing.stripe_card_token.nil? || property_listing.stripe_card_token.empty? %>
	 none
<% else %>
	<%= property_listing.card_type %> xxxx-xxxx-xxxx-<%= property_listing.last_four %><br>
<% end %>
<%= link_to "edit", edit_payment_info_property_path(property_listing) %>