<%= render 'service_mailer/email_header', title: "HumbleCasa: Service Request Scheduled"%>

<h2>Your service request for <%= @property.name %> has been assigned and scheduled!</h2>
<p>
  <h4> Schedule details:</h4>
  Scheduled date: <%= DateTime.parse(@service_request.first_scheduled.to_s).in_time_zone(@service_request.property.time_zone).strftime("%a %m/%d/%y %I:%M%p %Z") %><br>
  Assigned to: <%= @service_request.user.full_name %><br>
</p>           
<p>
  <%= render 'service_mailer/request_details' %>
</p>
<p>
  <%= render 'service_mailer/service_details' %>
</p>
<br>
<br>
<%= render 'service_mailer/closing_to_property' %>
<%= render 'service_mailer/email_footer' %>